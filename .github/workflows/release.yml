name: Release

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: release
  cancel-in-progress: false   # never abort a release mid-flight

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # push CHANGELOG commit + create release/tag
      packages: write     # push Docker image to ghcr.io

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0            # semantic-release needs full history
          persist-credentials: false

      - name: Configure git for release commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image name (lowercase)
        run: |
          echo "DOCKER_IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build and push Docker image (latest)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.DOCKER_IMAGE }}:latest

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Semantic release
        env:
          # GH_TOKEN must be a PAT (classic, repo scope) stored as a repository
          # secret. GITHUB_TOKEN cannot push to protected branches and does not
          # trigger downstream workflows. Create one at:
          #   https://github.com/settings/tokens
          # then add it to Settings → Secrets → Actions → New repository secret.
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npx semantic-release
